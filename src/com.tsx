import React from 'react';
const VscodeSchemaCore = (props) => (<span id='vscode-schme-core1' data-vscode-schema={props["data-vscode-schema"]}>{props.children}</span>)
// const HocVscode = ()
export {
    VscodeSchemaCore
}